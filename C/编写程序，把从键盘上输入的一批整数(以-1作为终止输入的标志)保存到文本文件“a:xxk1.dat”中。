
# include <iostream.h>
# include <fstream.h>
# include <stdlib.h>
 
void main() {
ofstream fout(“a:xxk1.dat”);  // 定义输出文件流并打开文件
if (!fout){
cerr <<”文件没有打开！”<<endl;
exit(1);
}        // 可有可无
int x;
cin>>x;
while (x!=-1) {
fout<<x<<’ ‘;
cin>>x;
}    // 能够从键盘向文件正确输出数据
fout.close();   // 关闭输出文件流
}
