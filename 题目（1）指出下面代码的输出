main()
{
int a[5]={1,2,3,4,5};
int *ptr=(int *)(&a+1);
printf("%d,%d",*(a+1),*(ptr-1));
}


///////////////////////
*(a+1) 就是a[1]  *(ptr-1)就是a[4]  执行结果为2,5

&a+1 不是首地址加1，系统会认为加一个数组的偏移，是偏移有一个数组的大小（这里为5）
int *ptr = (int*)(&a+1) 
ptr 实际上是&(a[5])



/////////////////////
char * const p;       常量指针，p的值不可以修改
char const *p         指向常量的指针，指向的常量值不可以修改
const char *p 
